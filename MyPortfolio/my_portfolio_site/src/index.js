import React from "react";
import { ReactDOM } from "react-dom";

ReactDOM.render(
    <React.StrictMode>
        firstPage();
        documemt.getElementById('root');
    </React.StrictMode>
)
const firstPage = () => {
    return (
        <main>
            <div>
                나의 포트폴리오 페이지 
            </div>
            <section>
                이건 section
            </section>
        </main>
    );
};